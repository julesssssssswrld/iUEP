# ── Java / Maven ──
target/
*.class
*.jar
*.war
.mvn/wrapper/maven-wrapper.jar

# ── IDE ──
.vscode/
.idea/
*.iml

# ── OS ──
Thumbs.db
.DS_Store

# ── Database ──
iuep.db
iuep.db-shm
iuep.db-wal

# ── Uploads (runtime images) ──
uploads/

# ── Environment / Secrets ──
.env

# ── Node.js (legacy) ──
node_modules/
package-lock.json

# ── Context ──
UEP-full-courses.txt